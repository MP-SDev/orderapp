function changeFirstLetterToUpperCase(string) {
    return string[0].toUpperCase() + string.slice(1);
}

function roundFloatNumberIntoString(number, decimalPlaces) {
    return number.toFixed(decimalPlaces);
}